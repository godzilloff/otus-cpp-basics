#pragma once

#include <iostream>
#include <fstream>
#include <string>

#include "types.h"

void game_init(config_game& config);
void game_over(config_game& config);
void game_play(config_game& config);
